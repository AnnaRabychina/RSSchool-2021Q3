import { buttons, pages } from './js/pages';

import {
  progress, btnVolume, toggleVolume, changeProgress, updateVolume

} from './js/volume';

import categories from './js/categoriesData';

import {
  categoriesContainer, pathCategories, fillCategories, insertCategory, changeCategory, openRound
} from './js/categories';

import { createRounds, rounds, uniqAnswers } from './js/rounds';
